apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboards
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  netflix-app.json: |
{{ .Files.Get "grafana/dashboards/netflix-app.json" | indent 4 }}

  node-exporter.json: |
{{ .Files.Get "grafana/dashboards/node-exporter.json" | indent 4 }}
