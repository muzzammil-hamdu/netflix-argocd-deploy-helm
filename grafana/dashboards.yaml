apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboards
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  netflix-app.json: |
    ${file:./dashboards/netflix-app.json}

  node-exporter.json: |
    ${file:./dashboards/node-exporter.json}
